image: pawankmr301/mycentosimage:tagname

stages:
    - build
    
buildstage:
    stage: build
    script:
        - build_artifact
        
.auto_devops: &auto_devops |
  # Auto DevOps variables and functions
  [[ "$TRACE" ]] && set -x
  
  
  function build_artifact () {
      mvn clean install
      curl  -u admin:admin123  --upload-file target/spring-eureka-server-0.0.1-SNAPSHOT.jar http://34.231.201.41:8081/nexus/content/repositories/123/
  }